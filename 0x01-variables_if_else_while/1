#include <stdio.h>


/**
 * main - entry point
 *
 * Description: declare the value
 *
 * return: 0(success)
*/

int main(void)
{
	int x, y, z, j;

	for (x = 0; x < 99; x++)
	{
		putchar(' ');
		for (y = x + 1; y <= 99; y++)
		{
			putchar(x + 48);
			putchar(x + 48);
			putchar(y + 48);
			putchar(y + 48);

			if (x <= 99 || y < 99)
			{
				putchar(',');
				putchar(' ');		
			}
		}
	}
	putchar('\n');
	return (0);
}		
