#include <stdio.h>
#include "main.h"

/**
 * print_sign - Print the sign of number
 *
 * Description: is the number false or true
 *
 * Return: 0 0r 1 0r 2
*/
int print_sign(int n)
{
	int n;

	n = 0;
	if (n > 0)
		_putcher(43);
		return (1);
	else if (n == 0)
		_putchar(48);
		return (0);
	else if
		_putchar(45);
		return (-1);
}
