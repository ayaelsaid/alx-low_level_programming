#include "main.h"

/**
 * _abs - Print the absolute value of an integer
 *
 * @n: take an integer for function
 *
 * Return: 0(success)
*/

int _abs(int n)
{
	if (x >= 0)
		_putchar(n);
	if (x < 0)
		_putchar(-1 * n);
	return (0);
}
