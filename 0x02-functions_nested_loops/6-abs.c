#include "main.h"

/**
 * _abs - Print the absolute value of an integer
 *
 * @n: take an integer for function
 *
 * Return: 0(success)
*/

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n
	return (n);
}
