#include <stdio.h>
#include "main.h"

/**
 * _islower - chect the lowercase
 *
 * Description: is c lowercase
 *
 * Return: return i if 'c' lower case, otherwise return 0 (success) 
*/
int _islower(int c)
{
	int c;

	if (c >= 97; c<= 122)
		return (1);
	return (o);
}
