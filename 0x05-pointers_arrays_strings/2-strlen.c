#include <stdio.h>
#include "main.h"

/**
 * _strlen - function that returns the length of a string
 *
 * @s: take a character
 *
 * Return: len
*/

int _strlen(char *s)
{
	int len;

	len = _strlen(s);
	return (len);
}
